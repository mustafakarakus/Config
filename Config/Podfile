source 'https://cdn.cocoapods.org/'

target 'Config' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Config

  target 'ConfigTests' do
    # Pods for testing
  end

end

target 'ConfigExamples' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ConfigExamples

end
